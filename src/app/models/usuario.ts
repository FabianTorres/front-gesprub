export interface Usuario {
  idUsuario: number;    
  nombreUsuario: string;  
  rolUsuario: string;     
  activo: number;         
  correo: string;
  fechaCreacion: string;   
  ultimoLogin: string;   
}